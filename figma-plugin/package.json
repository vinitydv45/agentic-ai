{
  "name": "aura-figma-plugin",
  "version": "1.0.0",
  "description": "Figma plugin to export design data to Aura backend for React conversion",
  "main": "dist/code.js",
  "scripts": {
    "build": "npm run build:code && npm run build:ui",
    "build:code": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es6",
    "build:ui": "node -e \"require('fs').copyFileSync('src/ui.html', 'dist/ui.html')\"",
    "watch": "npm run build && chokidar 'src/**/*' -c 'npm run build'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0",
    "chokidar-cli": "^3.0.0"
  },
  "keywords": [
    "figma",
    "plugin",
    "design-to-code",
    "react",
    "aura"
  ],
  "author": "Aura Team",
  "license": "MIT"
}
